1.What is the Shortcut Emmet Used to Create boilerplate of HTML ?
Answer:
html:5

2.What is DOCTYPE in Html ?
Answer:
<!DOCTYPE html> determine the version of website, but it tells the browser to use HTML5 standard.

3.What is Void Element and Example for Void Elements ?
Answer:
Void elements are HTML elements without closing tag
Example:
<br>
<img src="image.jpg">
<input type="text">

4.What is Different Element and Attributes ?
Answer:
Element: Complete tag 
Example:
<p>Hello</p>
Attributes: Extra information inside tag
Example:
<p style = "color:red;">Hello</p>

5.What is Html Entries and Why is it need in HTML ?
Answer:
Special Characters are HTML Entites
Which is used to display special characters
Example:
<p>CopyRight: &copy;</p>

6.What are meta tag and why is it used?
Answer:
Meta means Information and <meta> tags provide information about the webpage
which is  used for:
  *.SEO
  *.Character encoding
  *.Responsive design

7.What is the best way to add images in Website ?
Answer:
The best way is using ImageKit (CDN) because:
  *.It improves performance
  *.Loads images faster
  *.Optimizes images automatically